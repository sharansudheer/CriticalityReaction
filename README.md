# CriticalityReaction
A React Native Template App/Research(literally re-search, search and search till the internet is no more), directly compared with Native Kotlin functions(limitations are there like coroutines, low-level api etc). Android for now.
